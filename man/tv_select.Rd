% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{tv_select}
\alias{tv_select}
\alias{.tv_state}
\alias{tv_audit}
\alias{tv_compare}
\alias{tv_validate}
\alias{tv_missing_summary}
\alias{tv_filter}
\alias{tv_mutate}
\alias{tv_summarise}
\alias{tv_arrange}
\alias{tv_join}
\alias{tv_bind_rows}
\alias{tv_bind_cols}
\alias{tv_relocate}
\alias{tv_reshape}
\alias{tv_rename}
\alias{tv_rename_with}
\alias{tv_factor}
\alias{tv_collapse_levels}
\alias{tv_lump_levels}
\alias{tv_reorder_levels}
\alias{tv_relevel}
\alias{tv_dedupe}
\alias{tv_drop_na}
\alias{tv_slice}
\alias{tv_count}
\alias{tv_tabulate}
\alias{tv_crosstab}
\alias{tv_fill_na}
\alias{tv_separate}
\alias{tv_unite}
\title{Programmatic tidyview verbs}
\usage{
.tv_state(data, as)

tv_select(data, columns, as = deparse(substitute(data)))

tv_audit(data, top_n = 3L, as = deparse(substitute(data)))

tv_compare(
  data,
  other,
  by = NULL,
  as = deparse(substitute(data)),
  other_name = deparse(substitute(other))
)

tv_validate(data, rules, as = deparse(substitute(data)))

tv_missing_summary(data, group_by = NULL, as = deparse(substitute(data)))

tv_filter(
  data,
  conditions = list(),
  expr = NULL,
  logic = c("AND", "OR"),
  as = deparse(substitute(data))
)

tv_mutate(data, col_name, expr, as = deparse(substitute(data)))

tv_summarise(
  data,
  aggregations,
  group_by = NULL,
  as = deparse(substitute(data))
)

tv_arrange(data, sorts, as = deparse(substitute(data)))

tv_join(
  data,
  right,
  by,
  type = c("inner", "left", "right", "full", "semi", "anti"),
  as = deparse(substitute(data)),
  right_name = deparse(substitute(right))
)

tv_bind_rows(
  data,
  other,
  use_names = TRUE,
  fill = TRUE,
  as = deparse(substitute(data))
)

tv_bind_cols(data, other, as = deparse(substitute(data)))

tv_relocate(
  data,
  columns,
  before = NULL,
  after = NULL,
  as = deparse(substitute(data))
)

tv_reshape(
  data,
  direction = c("longer", "wider"),
  id_vars = NULL,
  measure_vars = NULL,
  formula = NULL,
  value_var = NULL,
  var_name = "variable",
  val_name = "value",
  as = deparse(substitute(data))
)

tv_rename(
  data,
  old = character(0),
  new = character(0),
  types = NULL,
  as = deparse(substitute(data))
)

tv_rename_with(
  data,
  columns = NULL,
  fn = c("lower", "upper", "trim", "snake", "prefix", "suffix", "replace",
    "replace_regex"),
  prefix = "",
  suffix = "",
  pattern = "",
  replacement = "",
  as = deparse(substitute(data))
)

tv_factor(
  data,
  column,
  mode = c("collapse", "lump", "reorder", "reference"),
  groups = list(),
  top_n = NULL,
  min_count = NULL,
  other_label = "Other",
  order_by = c("appearance", "alphabet_asc", "alphabet_desc", "freq_desc", "freq_asc",
    "custom"),
  levels = NULL,
  ref_level = NULL,
  new_col = NULL,
  as_factor = TRUE,
  as = deparse(substitute(data))
)

tv_collapse_levels(
  data,
  column,
  groups,
  new_col = NULL,
  as_factor = TRUE,
  as = deparse(substitute(data))
)

tv_lump_levels(
  data,
  column,
  top_n = NULL,
  min_count = NULL,
  other_label = "Other",
  new_col = NULL,
  as_factor = TRUE,
  as = deparse(substitute(data))
)

tv_reorder_levels(
  data,
  column,
  order_by = c("appearance", "alphabet_asc", "alphabet_desc", "freq_desc", "freq_asc",
    "custom"),
  levels = NULL,
  new_col = NULL,
  as = deparse(substitute(data))
)

tv_relevel(
  data,
  column,
  ref_level,
  new_col = NULL,
  as = deparse(substitute(data))
)

tv_dedupe(data, by_cols = NULL, as = deparse(substitute(data)))

tv_drop_na(data, columns = NULL, as = deparse(substitute(data)))

tv_slice(
  data,
  type = c("head", "tail", "sample"),
  n = 10L,
  as = deparse(substitute(data))
)

tv_count(data, by_cols = NULL, sort = FALSE, as = deparse(substitute(data)))

tv_tabulate(
  data,
  column,
  weight_expr = NULL,
  engine = c("data.table", "tsg", "auto"),
  sort = TRUE,
  percent = TRUE,
  cumulative = FALSE,
  convert_factor = FALSE,
  top_n = NULL,
  top_n_only = FALSE,
  add_total = TRUE,
  include_na = TRUE,
  position_total = c("bottom", "top"),
  output_name = paste0(deparse(substitute(data)), "_tab"),
  as = deparse(substitute(data))
)

tv_crosstab(
  data,
  row_var,
  col_var,
  weight_expr = NULL,
  normalize = c("none", "row", "col", "all"),
  totals = FALSE,
  engine = c("data.table", "tsg", "auto"),
  add_total = totals,
  add_total_row = totals,
  add_total_column = totals,
  add_percent = NULL,
  percent_by_column = NULL,
  convert_factor = FALSE,
  include_na = TRUE,
  position_total = c("bottom", "top"),
  output_name = paste0(deparse(substitute(data)), "_xtab"),
  as = deparse(substitute(data))
)

tv_fill_na(
  data,
  columns,
  method = c("value", "down", "up"),
  value = "",
  as = deparse(substitute(data))
)

tv_separate(
  data,
  column,
  into,
  sep = "_",
  remove = TRUE,
  as = deparse(substitute(data))
)

tv_unite(
  data,
  columns,
  into,
  sep = "_",
  remove = TRUE,
  as = deparse(substitute(data))
)
}
\arguments{
\item{data}{A data frame or data.table.}

\item{as}{Name to use in generated code. Defaults to the input name.}

\item{columns}{Source columns to combine.}

\item{top_n}{Optional top-\code{n} cutoff when using \code{tsg}.}

\item{other}{Second data source to combine with \code{data}.}

\item{by}{Character vector of join keys.}

\item{other_name}{Name to use in generated code for \code{other}.}

\item{rules}{A list of validation rules.}

\item{group_by}{Optional character vector of grouping columns.}

\item{conditions}{A list of condition descriptors like
\code{list(list(col = "region", op = "==", val = "North"))}.}

\item{expr}{Character R expression evaluated against the data.}

\item{logic}{Either \code{"AND"} or \code{"OR"}.}

\item{col_name}{New column name.}

\item{aggregations}{A list of aggregation descriptors like
\code{list(list(output = "sales", fn = "sum", col = "amount"))}.}

\item{sorts}{A list like \code{list(list(col = "region", desc = FALSE))}.}

\item{right}{Right-hand data source for joins.}

\item{type}{Slice method: \code{"head"}, \code{"tail"}, or \code{"sample"}.}

\item{right_name}{Name to use in generated code for the right table.}

\item{use_names}{Match columns by name when stacking rows.}

\item{fill}{Fill missing columns with \code{NA} when stacking rows.}

\item{before}{Optional anchor column to move before.}

\item{after}{Optional anchor column to move after.}

\item{direction}{Either \code{"longer"} or \code{"wider"}.}

\item{id_vars}{Identifier columns used for longer reshaping.}

\item{measure_vars}{Measure columns used for longer reshaping.}

\item{formula}{Formula used for wider reshaping.}

\item{value_var}{Value column used for wider reshaping.}

\item{var_name}{Output column name for variable labels in longer reshaping.}

\item{val_name}{Output column name for values in longer reshaping.}

\item{old}{Existing column names.}

\item{new}{New column names.}

\item{types}{Optional named list mapping final column names to target types.}

\item{fn}{Rename transform: \code{"lower"}, \code{"upper"}, \code{"trim"}, \code{"snake"},
\code{"prefix"}, \code{"suffix"}, \code{"replace"}, or \code{"replace_regex"}.}

\item{prefix}{Text to add before each selected column name.}

\item{suffix}{Text to add after each selected column name.}

\item{pattern}{Plain text to replace when \code{fn = "replace"}.}

\item{replacement}{Replacement text when \code{fn = "replace"}.}

\item{column}{Source column to split.}

\item{mode}{Factor transformation mode.}

\item{groups}{For collapse mode, a list like
\code{list(list(to = "Child", from = c("Infant", "Toddler")))}.}

\item{min_count}{For lump mode, keep categories with at least this count.}

\item{other_label}{Label to use for lumped categories.}

\item{order_by}{For reorder mode: \code{"appearance"}, \code{"alphabet_asc"},
\code{"alphabet_desc"}, \code{"freq_desc"}, \code{"freq_asc"}, or \code{"custom"}.}

\item{levels}{For reorder mode with \code{order_by = "custom"}, the desired
level order.}

\item{ref_level}{For reference mode, the level to move first.}

\item{new_col}{Optional output column. Defaults to overwriting \code{column}.}

\item{as_factor}{Store the result as a factor when supported by the mode.}

\item{by_cols}{Optional subset of columns defining duplicate rows.}

\item{n}{Number of rows.}

\item{sort}{Sort descending by frequency.}

\item{weight_expr}{Optional weighting expression evaluated against the data.}

\item{engine}{Either \code{"data.table"}, \code{"tsg"}, or \code{"auto"}.}

\item{percent}{Add a percent column.}

\item{cumulative}{Add a cumulative percent column.}

\item{convert_factor}{Convert labelled values to factors when using \code{tsg}.}

\item{top_n_only}{Keep only the top \code{n} categories when using \code{tsg}.}

\item{add_total}{Add totals when using \code{tsg}.}

\item{include_na}{Include missing values when using \code{tsg}.}

\item{position_total}{Position of totals when using \code{tsg}.}

\item{output_name}{Object name used in generated code for the result.}

\item{row_var}{Row dimension for a crosstab.}

\item{col_var}{Column dimension for a crosstab.}

\item{normalize}{Normalization mode: \code{"none"}, \code{"row"}, \code{"col"}, or \code{"all"}.}

\item{totals}{Add totals row and column.}

\item{add_total_row}{Add a total row when using \code{tsg}.}

\item{add_total_column}{Add a total column when using \code{tsg}.}

\item{add_percent}{Add percent columns when using \code{tsg}.}

\item{percent_by_column}{Calculate percentages by column when using \code{tsg}.}

\item{method}{Either \code{"value"}, \code{"down"}, or \code{"up"}.}

\item{value}{Replacement value for \code{method = "value"}.}

\item{into}{Output column name.}

\item{sep}{Separator inserted between values. Default \code{"_"}.}

\item{remove}{Drop the source columns after combining. Default \code{TRUE}.}
}
\value{
A transformed \code{data.table} or a tidyview summary object. In all cases, the
generated R code for the operation is stored in \code{attr(x, "tv_code")}.
}
\description{
Lightweight programmatic helpers that mirror the main tidyview GUI actions.
Each helper returns a \code{data.table} result or summary object and stores the
generated paste-ready R code in \code{attr(x, "tv_code")}.
}
\details{
Programmatic tidyview verbs

These helpers mirror the GUI operations with structured arguments and
return a \code{data.table} carrying the generated R code in \code{attr(x, "tv_code")}.

These helpers make it possible to use tidyview workflows without opening the
browser interface. They are useful when you want the same operation model as
the GUI, but you prefer to work directly in scripts, notebooks, or tests.

Most verbs return a transformed \code{data.table} and store the generated code in
\code{attr(x, "tv_code")}. Summary-style helpers such as \code{\link[=tv_audit]{tv_audit()}},
\code{\link[=tv_compare]{tv_compare()}}, \code{\link[=tv_validate]{tv_validate()}}, and \code{\link[=tv_missing_summary]{tv_missing_summary()}} return structured
result objects with the same \code{tv_code} attribute for reproducibility.
}
\section{Core transformation helpers}{

\itemize{
\item \code{\link[=tv_filter]{tv_filter()}} filters rows with structured conditions or a custom expression.
\item \code{\link[=tv_mutate]{tv_mutate()}} creates or replaces columns from an R expression.
\item \code{\link[=tv_summarise]{tv_summarise()}} computes grouped or ungrouped summaries.
\item \code{\link[=tv_arrange]{tv_arrange()}} sorts rows.
\item \code{\link[=tv_select]{tv_select()}} keeps a chosen subset of columns.
}
}

\section{Review and validation helpers}{

\itemize{
\item \code{\link[=tv_audit]{tv_audit()}} profiles columns, duplicates, and missingness.
\item \code{\link[=tv_compare]{tv_compare()}} compares two datasets and summarizes structural differences.
\item \code{\link[=tv_validate]{tv_validate()}} runs explicit validation rules and reports failures.
\item \code{\link[=tv_missing_summary]{tv_missing_summary()}} focuses on missing-data patterns.
}
}

\section{Reshaping and combination helpers}{

\itemize{
\item \code{\link[=tv_join]{tv_join()}}, \code{\link[=tv_bind_rows]{tv_bind_rows()}}, and \code{\link[=tv_bind_cols]{tv_bind_cols()}} combine datasets.
\item \code{\link[=tv_reshape]{tv_reshape()}} performs longer or wider reshaping.
\item \code{\link[=tv_separate]{tv_separate()}} and \code{\link[=tv_unite]{tv_unite()}} split or combine text columns.
\item \code{\link[=tv_relocate]{tv_relocate()}}, \code{\link[=tv_rename]{tv_rename()}}, and \code{\link[=tv_rename_with]{tv_rename_with()}} reorganize columns.
}
}

\section{Frequency and tabulation helpers}{

\itemize{
\item \code{\link[=tv_count]{tv_count()}} counts rows by group.
\item \code{\link[=tv_tabulate]{tv_tabulate()}} creates one-way frequency tables.
\item \code{\link[=tv_crosstab]{tv_crosstab()}} creates two-way cross-tabulations.
}
}

\examples{
dt <- data.table::as.data.table(mtcars)

filtered <- tv_filter(
  dt,
  conditions = list(list(col = "cyl", op = "==", val = "6"))
)
attr(filtered, "tv_code")

mutated <- tv_mutate(dt, col_name = "km_per_l", expr = "mpg * 0.425144")
names(mutated)

summary_dt <- tv_summarise(
  dt,
  aggregations = list(
    list(output = "avg_mpg", fn = "mean", col = "mpg"),
    list(output = "n_cars", fn = "n", col = "mpg")
  ),
  group_by = "cyl"
)

validation <- tv_validate(
  dt,
  rules = list(
    list(id = "rule1", type = "required", column = "mpg", label = "mpg is required")
  )
)
attr(validation, "tv_code")

}
\seealso{
\code{\link[=tidygui]{tidygui()}}, \code{\link[=tv_load]{tv_load()}}, \code{\link[=tv_get_psgc]{tv_get_psgc()}}, \code{\link[=tv_generate_frequency]{tv_generate_frequency()}},
\code{\link[=tv_generate_crosstab]{tv_generate_crosstab()}}
}
