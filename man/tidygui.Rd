% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidygui.R
\name{tidygui}
\alias{tidygui}
\title{Launch the tidyview GUI}
\usage{
tidygui(
  data = NULL,
  name = "DT",
  theme = m3_theme(),
  port = 7474L,
  launch.browser = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{data}{Optional. A data frame or data.table to open immediately.
If NULL, the import dialog is shown first.}

\item{name}{Character. The variable name to use in generated R code.
Defaults to "DT".}

\item{theme}{A theme object from \code{\link[=m3_theme]{m3_theme()}}. Defaults to the tidyview
purple seed theme.}

\item{port}{Integer. Preferred local port for the GUI server. Default 7474.
If that port is already in use, tidyview will try nearby ports
automatically.}

\item{launch.browser}{Logical. Open the browser automatically. Default TRUE.}

\item{quiet}{Logical. Suppress startup messages. Default FALSE.}
}
\value{
Invisibly returns the httpuv server handle. Call
\code{\link[=stop_tidygui]{stop_tidygui()}} to shut it down programmatically.
}
\description{
Opens a Material Design 3 browser-based GUI for guided data workflows.
Every interaction generates valid data.table R code shown live in the
code pane. The interface is designed to be teachable and safe for
non-technical users, with plain-language labels, guided builders, and
impact previews before destructive actions. Supports loading data from
the R environment or from files, including RCDF datasets when the
optional \code{rcdf} package is installed.
}
\details{
\code{tidygui()} is the main interactive entry point for the package. It starts
a local \code{httpuv} server, opens the browser interface, and keeps a live
script pane updated with the \code{data.table} code generated by each action.

The app is designed for guided data work. Instead of requiring users to
write code first, it helps them:
\itemize{
\item load data from the R environment or supported file types
\item inspect structure, labels, missingness, and duplicates
\item transform, validate, compare, and reshape data with guided panels
\item preview the likely impact of higher-risk actions before applying them
\item export the result while keeping the generated code visible
}

\code{tidygui()} works especially well for exploratory cleaning and review
workflows where users want both a point-and-click interface and a reproducible
code trail.
}
\section{Typical workflow}{

\enumerate{
\item Launch \code{tidygui()} with no data, or pass a dataset directly.
\item Load or inspect the active dataset in the app.
\item Apply guided operations such as filter, mutate, summarise, join,
validate, or pivot.
\item Review the generated R code in the script pane.
\item Export the transformed dataset or continue working in R.
}
}

\section{Notes}{

\itemize{
\item The browser interface uses tidyverse-style language, but the generated
script currently uses \code{data.table}.
\item When \code{port} is unavailable, tidyview automatically searches nearby
local ports.
\item Some import and export features use optional packages when available,
such as \code{readxl}, \code{haven}, \code{rcdf}, and \code{writexl}.
}
}

\examples{
\dontrun{
tidygui()
tidygui(mtcars, name = "cars")
tidygui(data.table::fread("sales.csv"), theme = m3_theme("#1D9E75"))
}

}
\seealso{
\code{\link[=stop_tidygui]{stop_tidygui()}}, \code{\link[=m3_theme]{m3_theme()}}, \code{\link[=tv_load]{tv_load()}}, \code{\link[=tv_filter]{tv_filter()}},
\code{\link[=tv_mutate]{tv_mutate()}}, \code{\link[=tv_summarise]{tv_summarise()}}, \code{\link[=tv_compare]{tv_compare()}}, \code{\link[=tv_validate]{tv_validate()}}
}
