% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\docType{package}
\name{tidyview-package}
\alias{tidyview-package}
\alias{tidyview}
\title{tidyview: Guided Data Workflows With a Browser-Based R Interface}
\description{
\code{tidyview} helps analysts inspect, clean, validate, reshape, compare, and
export data through a browser-based interface while keeping the generated R
code visible and reusable. It is designed for people who want a more guided
workflow without losing transparency about what the package is doing.
}
\details{
The main entry point is \code{\link[=tidygui]{tidygui()}}, which launches the local app and shows
generated paste-ready \code{data.table} code in a live script pane. The package
also includes programmatic helpers such as \code{\link[=tv_filter]{tv_filter()}}, \code{\link[=tv_mutate]{tv_mutate()}},
\code{\link[=tv_summarise]{tv_summarise()}}, \code{\link[=tv_compare]{tv_compare()}}, \code{\link[=tv_validate]{tv_validate()}}, \code{\link[=tv_tabulate]{tv_tabulate()}}, and
\code{\link[=tv_crosstab]{tv_crosstab()}} for workflows that need direct code access.

\code{tidyview} uses a tidyverse-style interface language in the GUI, but
currently generates and executes \code{data.table}-based code in the script pane.
The package aims to be teachable and transparent: most workflows explain
what they will do before applying a change, and the generated code remains
visible throughout the session.

For a typical session:
\enumerate{
\item load data from the R environment or a supported file format
\item inspect columns, missingness, and duplicates
\item transform or validate the data with guided panels
\item review the generated code
\item export the result or continue working programmatically
}
}
\section{Getting started}{

\itemize{
\item Use \code{\link[=tidygui]{tidygui()}} to launch the app with no data loaded yet.
\item Pass a data frame or \code{data.table} to \code{\link[=tidygui]{tidygui()}} to start with an
existing dataset.
\item Use the generated script pane to copy, review, and reuse the
operations you perform in the interface.
}
}

\section{Common workflows}{

\itemize{
\item inspect column types, labels, duplicates, and missingness
\item filter, select, arrange, and mutate with guided controls
\item recode categories and validate business rules
\item summarise, tabulate, and crosstab data
\item join, compare, separate, unite, and reshape datasets
\item add PSGC area names from bundled lookup data
\item export transformed datasets with generated code and file previews
}
}

\section{Main functions}{

\itemize{
\item \code{\link[=tidygui]{tidygui()}} launches the browser app
\item \code{\link[=m3_theme]{m3_theme()}} customizes the app theme
\item \code{\link[=tv_fread]{tv_fread()}} and \code{\link[=tv_load]{tv_load()}} help bring data into workflow
\item \code{\link[=tv_audit]{tv_audit()}} and \code{\link[=tv_missing_summary]{tv_missing_summary()}} support data review
\item \code{\link[=tv_validate]{tv_validate()}} adds explicit validation rules
\item \code{\link[=tv_generate_frequency]{tv_generate_frequency()}} and \code{\link[=tv_generate_crosstab]{tv_generate_crosstab()}} expose
optional \code{tsg} integrations
}
}

\section{Optional integrations}{

Some workflows use optional packages when available, including \code{readxl},
\code{haven}, \code{rcdf}, \code{tsg}, \code{writexl}, and \code{rstudioapi}. PSGC area-name lookup
is bundled in the package and does not require a separate PSGC package.
}

\section{Generated code}{

The interface uses tidyverse-style language in the GUI, but the script pane
currently generates and executes \code{data.table}-based code.
}

\examples{
\dontrun{
library(tidyview)

# Launch the app with no data loaded yet
tidygui()

# Launch the app with an existing data frame
tidygui(mtcars, name = "mtcars")

# Programmatic helpers also return generated code
cars <- tv_filter(
  data.table::as.data.table(mtcars),
  list(list(col = "cyl", op = "==", val = "6"))
)
attr(cars, "tv_code")
}

}
\seealso{
\code{\link[=tidygui]{tidygui()}}, \code{\link[=tv_filter]{tv_filter()}}, \code{\link[=tv_mutate]{tv_mutate()}}, \code{\link[=tv_summarise]{tv_summarise()}},
\code{\link[=tv_compare]{tv_compare()}}, \code{\link[=tv_validate]{tv_validate()}}, \code{\link[=tv_audit]{tv_audit()}},
\code{\link[=tv_missing_summary]{tv_missing_summary()}}, \code{\link[=tv_generate_frequency]{tv_generate_frequency()}},
\code{\link[=tv_generate_crosstab]{tv_generate_crosstab()}}
}
\author{
\strong{Maintainer}: Elmer Tumlos \email{e.tumlos@psa.gov.ph}

}
\keyword{package}
